---
---
<section class="relative w-full h-screen flex items-center justify-center bg-[url('/Pulse/pulse-hero-wallpaper.png')] bg-cover bg-center text-center text-white overflow-hidden">
  <div class="absolute inset-0 bg-radial-[circle_at_center] from-black/20 to-black/60 z-1"></div>
  
  <div class="relative z-10 max-w-4xl px-8">
    <img 
      src="/Pulse/Pulse-iOS-Default-1024x1024@1x.png" 
      alt="Pulse App Icon" 
      class="w-24 h-24 md:w-32 md:h-32 mx-auto mb-8 drop-shadow-2xl animate-in fade-in slide-in-from-bottom-2 duration-1000"
    />
    <h1 class="text-7xl md:text-8xl font-extrabold tracking-tight drop-shadow-2xl animate-in fade-in slide-in-from-bottom-4 duration-1000 delay-100">
      Pulse
    </h1>
    <p class="mt-6 text-xl md:text-2xl text-white/70 leading-relaxed font-normal animate-in fade-in slide-in-from-bottom-6 duration-1000 delay-300">
      A beautiful, non-intrusive service monitor that lives in your notch.<br class="hidden md:block">
      Supports Better Stack, Atlassian Statuspage, and custom HTTP health checks.
    </p>
    
    <div class="mt-12 inline-block text-left min-w-[320px] bg-black/60 backdrop-blur-md border border-white/10 rounded-xl overflow-hidden shadow-2xl animate-in fade-in slide-in-from-bottom-8 duration-1000 delay-600">
      <div class="bg-white/5 px-3 py-2 flex items-center justify-between border-b border-white/5">
        <div class="flex gap-1.5">
          <div class="w-2.5 h-2.5 rounded-full bg-[#ff5f56]"></div>
          <div class="w-2.5 h-2.5 rounded-full bg-[#ffbd2e]"></div>
          <div class="w-2.5 h-2.5 rounded-full bg-[#27c93f]"></div>
        </div>
      </div>
      <div class="p-4 md:p-5 font-mono text-sm md:text-base flex items-center justify-between gap-4">
        <div class="flex items-center">
          <span class="text-green-500 mr-2 select-none">$</span>
          <span id="install-command">brew install --cask jsattler/tap/pulse</span>
        </div>
        <button 
          id="copy-button"
          class="p-1.5 rounded-md hover:bg-white/10 transition-colors text-white/40 hover:text-white"
          title="Copy to clipboard"
        >
          <svg id="copy-icon" xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>
          <svg id="check-icon" class="hidden" xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#22c55e" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
        </button>
      </div>
    </div>

    <div class="mt-8 animate-in fade-in slide-in-from-bottom-10 duration-1000 delay-800">
      <a 
        href="https://github.com/jsattler/Pulse/releases/latest" 
        target="_blank" 
        rel="noopener noreferrer"
        class="inline-flex items-center gap-2 text-sm font-medium text-white/40 hover:text-white transition-colors"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" x2="12" y1="15" y2="3"/></svg>
        Download from GitHub
      </a>
    </div>
  </div>
</section>

<script>
  const copyButton = document.getElementById('copy-button');
  const installCommand = document.getElementById('install-command');
  const copyIcon = document.getElementById('copy-icon');
  const checkIcon = document.getElementById('check-icon');

  copyButton?.addEventListener('click', () => {
    const text = installCommand?.innerText || 'brew install --cask jsattler/tap/pulse';
    navigator.clipboard.writeText(text).then(() => {
      copyIcon?.classList.add('hidden');
      checkIcon?.classList.remove('hidden');
      
      setTimeout(() => {
        copyIcon?.classList.remove('hidden');
        checkIcon?.classList.add('hidden');
      }, 2000);
    });
  });
</script>
